{
  "name": "@oasisprotocol/docs-js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "docs": "typedoc",
    "docs-all": "npm run docs --workspaces --if-present"
  },
  "dependencies": {
    "typedoc": "^0.25.1"
  }
}
